# Incident Report — 2026-06-05

## 요약
- 발생 시간: 07:36
- 복구 완료: 14:34
- 누락 데이터: 36건 (복구 완료)

## 타임라인
| 시간 | 내용 |
|------|------|
| 07:05 | 결제조회 0건 (잠재 문제 첫 발현) |
| 07:36 | 로그인 실패 - 셀렉터 strict mode violation |
| 10:24 | 로그인 Timeout 30초 초과 |
| 10:29 | 브라우저 WinError 2 - watchdog 환경 불일치 |
| 13:16 | 로그인 셀렉터 수정 후 성공 확인 |
| 13:26 | watchdog .venv 경로로 재기동 |
| 14:34 | 결제조회 진입 안정화 수정 후 36건 복구 완료 |

## 근본 원인
1. 로그인 셀렉터 불일치
   - `input[name]` 기반 선택자가 hidden input까지 함께 잡아 strict mode violation 발생
   - 비밀번호 필드는 `name="passwd"`가 없어 timeout 발생
2. watchdog 재시작 시 Python 환경 불일치
   - `pythonw`가 시스템 Python(`C:\Python314`)으로 호출됨
   - Playwright 버전 불일치로 `[WinError 2]` 발생
3. 결제내역조회 페이지 진입 타이밍 문제
   - 로그인 후 ERP 셸 준비 전에 해시 변경/그리드 확인을 시도
   - 그리드 미감지 상태를 조회 0건으로 오탐

## 해결 내용
1. `modules/login.py` - 로그인 셀렉터를 id 기반으로 변경
2. `watchdog.py` - 재시작 시 `sys.executable` 사용
3. `start_watchdog.bat` - `.venv` 경로 하드코딩
4. `modules/reader.py` - 메뉴 클릭 우선 + 해시 fallback + 그리드 재확인
