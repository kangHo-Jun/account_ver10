# 기술 컨텍스트 — 2026-06-05 변경사항

## 변경된 파일
| 파일 | 변경 내용 |
|------|-----------|
| modules/login.py | 셀렉터 `name` -> `id` 기반으로 변경 |
| modules/reader.py | 페이지 진입 검증 + 타이밍 안정화 |
| watchdog.py | `sys.executable` 사용 |
| start_watchdog.bat | `.venv` 경로 하드코딩 |

## 주요 기술 결정
- 로그인: `input#com_code` / `input#id` / `input#passwd` 사용
- 페이지 진입: 메뉴 링크 클릭 우선, 실패 시 해시 fallback
- 그리드 확인: `wait_for_selector` + 재시도 2회
- Python 환경: `.venv` 고정 (Playwright 1.59.0)

## 알려진 잠재 문제
- `venv` 환경 Playwright가 깨진 상태 (`WinError 2`) - 미해결
- 메뉴 링크(`a#link_depth4_MENUTREE_002905`)는 ERP 업데이트 시 변경 가능성 있음
